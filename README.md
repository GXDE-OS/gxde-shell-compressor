# GXDE Shell Compressor

```
Usage: gxde-shell-compressor [-f | --extract] | [-d | --compress] | [-h | --extract-here]
  -f, --extract          解压文件并提示进度
  -d, --compress         压缩文件并选择格式
  -h, --extract-here     解压到文件所在目录
  --help                 显示帮助信息

```